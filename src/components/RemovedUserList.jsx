// import React from 'react';

// const RemovedUserList = ({ users, onRestore }) => {
//   return (
//     <ul className="space-y-2">
//       {users.map((user) => (
//         <li key={user.id} className="flex justify-between items-center bg-gray-200 p-2 rounded-md shadow">
//           <span>{user.name} - {user.email}</span>
//           <button onClick={() => onRestore(user)} className="bg-green-500 text-white px-3 py-1 rounded hover:bg-green-600">Restore</button>
//         </li>
//       ))}
//     </ul>
//   );
// };

// export default RemovedUserList;
